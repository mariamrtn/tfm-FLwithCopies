name: flower-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - flwr==1.26.1
    - torch==2.8.0
    - torchvision
    - pandas
    - numpy
    - matplotlib
    - flwr-datasets
    - datasets